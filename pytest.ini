[pytest]
# Test discovery + coverage configuration.
# Run the suite with:  pytest
# Coverage is measured over the application package only.
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing
